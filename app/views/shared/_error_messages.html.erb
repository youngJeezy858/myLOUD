<% if target.errors.any? %>
<div id="error_explanation">
  <div class="alert alert-danger">
    <h4><%= pluralize(target.errors.count, "error") %> prohibited this from being created:</h4><br/>
    <ul>
      <% target.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
<% end %>
